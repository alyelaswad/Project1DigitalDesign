#ifndef LOGICGATES_H
#define LOGICGATES_H
class LogicGates
{
public:
    bool AND(bool, bool);

    bool OR(bool, bool);

    bool NOT(bool);

    bool NAND(bool, bool);

    bool NOR(bool, bool);
};

#endif
